{{!< default}}

{{#foreach posts}}
    {{#if @first}}
        {{> list-post}}
    {{else}}
        {{> list-post}}
    {{/if}}
{{/foreach}}


{{!! After all the posts, show the newer/older pagination links }}
{{#if pagination}}
    {{pagination}}
{{/if}}
