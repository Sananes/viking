{{!< default}}

{{#foreach posts}}
    {{#if @first}}
        {{> list-post}}
    {{else}}
        {{> list-post}}
    {{/if}}
{{/foreach}}

<div class="endless" style="text-align: center; display: none;">
    <a id="loading" href="#"><span>Loading...</span></a>
</div>
{{!! After all the posts, show the newer/older pagination links }}
{{#if pagination}}
    {{pagination}}
{{/if}}
